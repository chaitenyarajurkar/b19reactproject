import React from 'react';
import Carousel from './Carousel';

const index = () => {
    return (
        <div>
            <Carousel></Carousel>
            <h1>This is Dashboard</h1>
        </div>
    );
};

export default index;